import {Router} from 'express';import {auth} from '../middleware/auth.js';import {pool} from '../db/pool.js';import XLSX from 'xlsx';
const r=Router();r.get('/',auth,async(req,res)=>{const q=await pool.query('SELECT meal_date AS date, breakfast, lunch, dinner, snacks, notes FROM meal_records WHERE user_id=$1 ORDER BY meal_date',[req.user.id]);const rows=q.rows.map(x=>({...x,date:String(x.date),Breakfast:x.breakfast?'Yes':'No',Lunch:x.lunch?'Yes':'No',Dinner:x.dinner?'Yes':'No',Snacks:x.snacks?'Yes':'No'}));const data=rows.map(x=>({Date:x.date,Breakfast:x.Breakfast,Lunch:x.Lunch,Dinner:x.Dinner,Snacks:x.Snacks,Notes:x.notes}));const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(data),'Food Tracker');const buf=XLSX.write(wb,{type:'buffer',bookType:'xlsx'});res.setHeader('Content-Type','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');res.setHeader('Content-Disposition','attachment; filename="food-tracker.xlsx"');res.send(buf)});export default r;